import React from 'react';
import DataProvider from '../data/DataProvider';

const Chart = () => {
  return (
    <DataProvider>
      <div>Chart</div>
    </DataProvider>
  );
};

export default Chart;
